/** @type {import('next').NextConfig} */
const assetPrefix = `/`;
const basePath = `/`;

const nextConfig = {
    assetPrefix: assetPrefix,
    basePath: basePath,
    output: "export",
};

module.exports = nextConfig;
